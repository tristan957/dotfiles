when:
  - event: [push, manual]
    branch: [master]

engine: microvm

steps:
  - name: download-dependencies
    run: nix develop --command true

  - name: check-flake
    run: nix flake check --print-build-logs
