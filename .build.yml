image: alpine/edge
packages:
  - reuse
  - ruff
  - shellcheck
  - shfmt
  - stylua
sources:
  - https://git.sr.ht/~tristan957/dotfiles
tasks:
  - reuse: |
      cd dotfiles
      reuse lint
  - ruff: |
      cd dotfiles
      ./scripts/ruff.sh
  - shellcheck: |
      cd dotfiles
      ./scripts/shellcheck.sh
  - shfmt: |
      cd dotfiles
      ./scripts/shfmt.sh
  - stylua: |
      cd dotfiles
      stylua --check .
