# TODO

Potential reference files to add as needed:

- **Catalog changes** — `pg_*.dat` files, BKI, upgrade implications.
- **WAL** — adding WAL records, resource managers, redo routines.
- **SPI** — running SQL from extensions, context switching, error handling.
- **Node system** — adding node types, `Node` tags, copy/equal/read/out support.
- **PL/pgSQL** — expression evaluation, variable scoping, SPI usage within the
  PL.
