when:
  - event: [push, manual]
    branch: [master]

engine: microvm

steps:
  - name: download-dependencies
    command: nix develop --command true

  - name: check-flake
    command: nix flake check --print-build-logs
